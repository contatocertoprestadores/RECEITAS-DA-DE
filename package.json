{
  "name": "fabrica-de-salgados",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html manifest.json sw.js icon-*.png public/ 2>/dev/null || cp *.png public/ 2>/dev/null; cp public/index.html ./ 2>/dev/null; echo 'build ok'"
  }
}